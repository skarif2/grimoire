---
description: Handoff — index key session decisions, then write a handoff doc for the next agent to pick up
argument-hint: "[what the next session will focus on]"
---
$ARGUMENTS

Load and follow the `handoff` skill to capture this idea for a fresh session.

The skill will:
1. Index the new idea into the knowledge base so it's discoverable later
2. Write a focused handoff document to `~/GRIMOIRE/docs/[group]/[project]/handoffs/` (opened automatically)
3. Include just enough context for a fresh agent to start cold — no session summary
